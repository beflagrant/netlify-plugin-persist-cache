name: netlify-plugin-persist-cache
inputs:
  - name: source
    description: The source directory
    default: '.'
  - name: cacheFolder
    description: The folder inside the source to be cached
    default: '.jekyll-cache'
